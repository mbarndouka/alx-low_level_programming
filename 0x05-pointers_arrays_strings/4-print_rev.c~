#include "main.h"
/**
 *print_rev - print the reverse of string
 *@s: string to print it reverse
 *Return: void
 */
void print_rev(char *s)
{
int length = 0;
while (*s != '\0')
{
length++;
s++;
}
str--;
while (length > 0)
{
_putchar(*s);
s--;
length--;
}
_putchar('\n');
}
